{
  "version": 2,
  "buildCommand": "npm run force-build",
  "ignoreCommand": "npx --no -- prisma generate",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "skipBuild": true
      }
    }
  ]
} 